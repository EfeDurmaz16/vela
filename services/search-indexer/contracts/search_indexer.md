# Search Indexer Contract

Methods:

- `IndexRepository(repo_id, snapshot_id)`
- `SearchCode(repo_id, query)`
- `SearchSymbols(repo_id, query)`
- `SearchEvidence(org_id, filters)`
- `DeleteRepositoryIndex(repo_id)`
