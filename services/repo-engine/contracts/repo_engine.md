# Repo Engine Contract

Methods:

- `GetRepositorySnapshot(repo_id, sha)`
- `GetBranch(repo_id, name)`
- `ListBranches(repo_id)`
- `GetCommit(repo_id, sha)`
- `GetTree(repo_id, tree_hash)`
- `GetBlob(repo_id, blob_hash)`
- `CreateSnapshot(repo_id, ref, sha)`

Phase 0 uses mock code and diff placeholders.
